<div class="container">
  <div class="product-detail">
    <div class="detail-header">
      <h1>📦 <%= @product.name %></h1>
    </div>
    
    <div class="detail-actions">
      <%= link_to "✏️ Edit Product", edit_product_path(@product), class: "btn btn-primary" %>
      <%= link_to "← Back to Products", products_path, class: "btn" %>
    </div>
  </div>
</div>