<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      .container { max-width: 600px; margin: 0 auto; font-family: Arial, sans-serif; }
      .header { background-color: #f8f9fa; padding: 20px; text-align: center; }
      .content { padding: 20px; }
      .product-info { background-color: #e8f5e8; padding: 15px; border-radius: 5px; margin: 20px 0; }
      .button { display: inline-block; background-color: #007bff; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; margin: 10px 0; }
      .footer { background-color: #f8f9fa; padding: 20px; text-align: center; font-size: 12px; color: #666; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>🎉 <%= t('stock_notification_mailer.back_in_stock.heading') %></h1>
      </div>
      
      <div class="content">
        <p><%= t('stock_notification_mailer.back_in_stock.greeting') %></p>
        
        <div class="product-info">
          <h2>📦 <%= @product.name %></h2>
          <p><%= t('stock_notification_mailer.back_in_stock.availability', count: @product.stock_quantity) %></p>
          
          <% if @product.description.present? %>
            <div>
              <%= truncate(@product.description.to_plain_text, length: 200) %>
            </div>
          <% end %>
        </div>
        
        <p><%= t('stock_notification_mailer.back_in_stock.call_to_action') %></p>
        
        <a href="<%= product_url(@product) %>" class="button">
          <%= t('stock_notification_mailer.back_in_stock.view_product') %>
        </a>
      </div>
      
      <div class="footer">
        <p><%= t('stock_notification_mailer.back_in_stock.footer') %></p>
        <p>
          <a href="<%= product_stock_notification_url(@product, @stock_notification) %>" data-method="delete">
            <%= t('stock_notification_mailer.back_in_stock.unsubscribe') %>
          </a>
        </p>
      </div>
    </div>
  </body>
</html>